using system;

namespace Aula6vsc
{
  class program
  {
    static void Main(string[]args)
    {
      console.WriteLine("Tabuada");

      for(int x = 1; x <= 10; x++);
      {
        for(int y = 1; y <=10; y++)
        {
          Console.WriteLine(x + " x " + y + "-" + x * y);
        }
      }
    }
  }
}
